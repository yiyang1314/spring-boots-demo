
      #schema: database/import.sql
      #sql-script-encoding: utf-8
spring:
    messages:
       basename: i18n/Messages,i18n/Pages
    datasource:
      url: jdbc:mysql://localhost:3306/t_roles?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=true
      username: root
      password: 
      driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      database: mysql
      show-sql: true
      hibernate:
        ddl-auto: update
      properties:
         hibernate:
            dialect: org.hibernate.dialect.MySQL5Dialect
